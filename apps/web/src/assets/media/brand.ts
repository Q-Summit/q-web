/** Site brand marks on the Worker/R2 `/media/*` tree (not committed binaries). */

/** Full Q + SUMMIT / University of Mannheim lockup (wide, not square). */
export const logoSrc = "/media/q-summit-logo-lockup.webp";

/**
 * Intrinsic pixel size of logoSrc (tight crop of Blau-Q-Summit.png).
 * Keep in sync when re-exporting the lockup.
 */
export const logoWidth = 1794;
export const logoHeight = 376;

/** Square Q mark only - keep for places that need a 1x1 icon. */
export const logoMarkSrc = "/media/6755c07042d0c9f23a33c94c_Logo-Blau.webp";
